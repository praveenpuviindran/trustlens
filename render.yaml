services:
  - type: web
    name: trustlens-api
    env: python
    buildCommand: pip install -e .
    startCommand: uvicorn trustlens.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: CORS_ORIGINS
        value: ""
      - key: PYTHON_VERSION
        value: "3.11.14"
